"""
commands/_template.py — шаблон для новой команды

Скопируй этот файл, переименуй (без _) и заполни.
Пример: commands/send_email.py, commands/play_spotify.py

Правила:
  - Имя файла = название команды (примерно)
  - Не начинать с _ (иначе автосборщик пропустит)
  - Обязательно: COMMAND_NAME, DESCRIPTION, PARAMETERS, REQUIRED, handler()
"""

# ── 1. Название команды (GPT использует это имя для вызова) ───────────────────
COMMAND_NAME = "my_command"

# ── 2. Описание — GPT читает это чтобы понять когда вызывать команду ─────────
DESCRIPTION = "Что делает эта команда и когда её нужно использовать"

# ── 3. Параметры в формате JSON Schema (если нет параметров — оставь {}) ──────
PARAMETERS = {
    "param1": {
        "type":        "string",
        "description": "Описание параметра",
    },
    "param2": {
        "type":        "integer",
        "description": "Числовой параметр (необязательный)",
        "default":     0,
    },
}

# ── 4. Список обязательных параметров ─────────────────────────────────────────
REQUIRED = ["param1"]


# ── 5. Обработчик — принимает параметры из GPT, возвращает строку ─────────────
def handler(param1: str, param2: int = 0) -> str:
    # Твой код здесь
    result = f"Выполнено: {param1}"
    return result

